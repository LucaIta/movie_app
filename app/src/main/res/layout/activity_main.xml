<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.epicodus.movieapp.ui.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/userInputEditText"
        android:layout_marginBottom="40dp"
        android:hint="Find movies by title, rating, or release date"
        android:textSize="14sp"
        android:layout_above="@+id/findMovieInfoButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Find Movie Info"
        android:id="@+id/findMovieInfoButton"
        android:layout_marginBottom="100dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Find Movies Here!!!"
        android:id="@+id/titleTextView"
        android:textSize="36sp"
        android:layout_marginBottom="100dp"
        android:layout_above="@+id/userInputEditText"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
